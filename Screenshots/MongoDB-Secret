Kubernetes pulls the username and password from a Secret(a secure way sensitive data is stored E.g passwords,tokens or keys) and passes them into the container as environment variables(key-value pairs e.g Username=Debs) , instead of hardcoding them in the YAML.
MongoDB then reads those variables on startup to securely create the admin user without ever exposing the credentials in plain text in the file.
